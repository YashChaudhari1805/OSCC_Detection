"""
api/routes.py — DEAD CODE / FLASK REMNANT
-----------------------------------------
This file is NOT used by the application.

The project runs on FastAPI (main.py). All routes are defined there.
This file was left over from an earlier Flask prototype and uses Flask
imports (`flask`, Blueprint, `current_app`) that are NOT installed.

ACTION REQUIRED: Delete this file (and the api/ directory) entirely.
Keeping it causes confusion and will break any tool that auto-discovers
routes (e.g. certain IDE plugins).
"""

raise ImportError(
    "api/routes.py is a Flask remnant and must not be imported. "
    "Delete this file. All routes live in main.py (FastAPI)."
)
